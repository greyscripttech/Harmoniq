import 'package:flutter/material.dart';

class AnimatedWave extends StatelessWidget {
  const AnimatedWave({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // We'll animate this in next step
  }
}
